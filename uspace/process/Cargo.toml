[package]
name = "weensyos-process"
version = "0.1.0"
authors = ["Anton Melnychuk <anton.melnychuk@yale.edu>"]
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
weensyos-shared = { path = "../../shared/rust" }
